{
  "name": "personal-portfolio",
  "homepage": "https://aarushth.github.io/",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "vite --host --port 8080",
    "build": "vite build",
    "preview": "vite preview --host --port 8080"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.0.4",
    "vite-plugin-compression": "^0.5.1"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.178.0"
  }
}
